function solve(inputArgs) {
    const carCommands = function(name) {
        const car = {
            'name': name,
            
        };
    }();
}

solve(['create c1',
'create c2 inherit c1',
'set c1 color red',
'set c2 model new',
'print c1',
'print c2']
);